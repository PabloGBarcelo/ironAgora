<div class="navbar">
  <img src="/images/logo.png">
</div>
<ul>
  <% users.forEach(user => { %>
    <li>
      <img src="<%= user.avatar %>" alt="<%= user.name %>">
      <a href="/user/<%= user._id %>"><%= user.name %></a>
    </li>
  <% }); %>
</ul>
